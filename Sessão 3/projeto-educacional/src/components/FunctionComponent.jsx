const FunctionComponent = ({MyFunction}) => {
  return (
    <div>
        <h3>Veja um exemplo de Função em props</h3>
        <button onClick={MyFunction}>Clique em mim</button>
    </div>
  )
}

export default FunctionComponent